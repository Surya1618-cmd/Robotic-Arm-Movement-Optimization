algorithm: DoubleDQN
use_dueling: true
use_per: true

episodes: 1500
max_steps: 200

gamma: 0.99
learning_rate: 0.0005
batch_size: 64

epsilon_start: 1.0
epsilon_min: 0.1
epsilon_decay: 0.995

target_update_freq: 200
tau: 0.005
